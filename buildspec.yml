version: 0.2

phases:
  install:
    commands:
      - echo "No dependencies to install for a static site."
  build:
    commands:
      - echo "Building (nothing to compile) ..."

artifacts:
  files:
    - '**/*'
  base-directory: .
``
